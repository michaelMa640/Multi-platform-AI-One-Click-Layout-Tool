# Tauri Runtime Skeleton

Step 1 先把前端页面壳和目录结构搭起来。

当前目录用于预留 Tauri 运行时结构，后续在本机补齐 Rust / Cargo / Tauri CLI 之后，再初始化完整的桌面端运行配置。

建议下一步：

1. 安装 Rust toolchain
2. 安装 Tauri CLI
3. 初始化正式的 `Cargo.toml` 和 Tauri capability/config 文件
4. 将前端 dev/build 命令接入 Tauri 桌面运行链路
